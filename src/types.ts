// Define types here

export type ButtonStyle = {
  color: string;
  icon: React.ReactNode;
};

export type UserAuth = {
  id: number;
  username: TimeRanges;
  password: string;
};

export interface InterfaceSample {
  damage: number;
}
